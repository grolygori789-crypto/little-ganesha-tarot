# Little Ganesha Tarot — QA V0.3.6

**Build:** 0.3.6  
**Master Plan at build creation:** V3.6  
**Current Master Plan after post-deploy governance update:** V3.7  
**GitHub baseline:** `main` commit `ce0ad2c4554c9cea80907c4f363d64ac5821682d` — `Rebuild home visual system V0.3.5`  
**Release type:** Premium Home Polish / regression-protected overlay

## Executive result

**PRE-DEPLOY RELEASE GATE: PASS**

- GitHub baseline hash verification: **PASS**
- Static / structural automated checks: **198 / 198 PASS**
- Simulated Chromium application-flow checks: **22 / 22 PASS**
- Simulated responsive layout checks: **150 / 150 PASS**
- Total automated/simulated checks represented in this release gate: **370 / 370 PASS**

## 1. GitHub baseline verification

Before V0.3.6 was rebuilt, the V0.3.5 working baseline was compared against GitHub `main` using Git blob hashes. The following core files matched exactly:

- `index.html` → `214ac5d2a61fe0a8fede77d0f9a33a30480b63c9`
- `css/app.css` → `12fe6c46fc98c23959839eac1cef5cc940103b93`
- `js/app.js` → `bc52a7786b0719085dc4a9d751e4ddc9da241649`
- `js/audio.js` → `d0b67ed72407b46cbd9507f5eb173850962c5efe`
- `js/pwa.js` → `0a9967e46ad3bc507f15d328bdbd478b6c06adcb`
- `sw.js` → `3fff20db2abc3dfffac28fe8bc059efe63d37ae0`
- `manifest.webmanifest` → `024133c3cb6e7e8423475cf6cc7c483dfa9120e8`
- `MASTER_PLAN.md` → `cbf59614c42b1e29f0a3c43b110d356eb923822f`
- all eight `assets/motifs/*.svg` files also matched the GitHub blobs before modification.

This establishes that the patch was rebuilt from the actual deployed/repository V0.3.5 source rather than from an unverified local approximation.

## 2. Protected systems

The following systems/files remain byte-identical to the verified V0.3.5 baseline and are intentionally **not included in the upload patch**:

- `js/audio.js`
- `js/pwa.js`
- `manifest.webmanifest`
- `data/AUDIO_MANIFEST_V1.json`
- both current MP3 tracks
- PWA launcher/icon assets
- canonical title hero and card back
- 78 canonical card fronts

V0.3.6 does not refactor the audio engine, PWA registration logic, profile storage model, navigation model, Support placeholders, or tarot asset system.

## 3. V0.3.6 change scope

Changes are limited to:

- cache/build markers (`0.3.5` → `0.3.6`)
- Home CSS polish layer
- Thai personalized-greeting punctuation
- five refined Home motif SVGs
- service-worker version/cache-bust values
- Master Plan / release / QA documentation

`js/app.js` was normalized back against V0.3.5 during QA and confirmed to contain only the intended greeting-format change plus build-marker change.

`sw.js` was normalized back against V0.3.5 and confirmed to contain only build/cache-bust version changes.

The V0.3.6 CSS is appended after the byte-identical V0.3.5 CSS baseline to keep the polish layer isolated and auditable.

## 4. Static / structural QA — 198 / 198 PASS

Validated areas include:

- JavaScript syntax via `node --check`
- HTML ID uniqueness and JS DOM-linkage targets
- ARIA labelled-by target linkage
- CSS parse integrity via `tinycss2`
- CSS brace integrity
- absence of the rejected generic child-position regression rule
- SVG XML parsing for all eight motif files
- PWA manifest required fields and icon linkage
- service-worker app-shell resource linkage
- relative GitHub Pages-safe PWA scope/start URL
- canonical title-hero hash verification against repository identity
- protected-file byte equality
- target-width design guards
- local HTTP resource smoke checks
- Master Plan V3.6 authority/version closure

## 5. Simulated application flow — 22 / 22 PASS

A headless Chromium DOM simulation used the real V0.3.6 `index.html`, CSS and `app.js`, with isolated mocks only for browser-external audio/local-storage/fullscreen services.

Verified flow:

1. Benedict splash retires and Title reveals.
2. `TAP TO BEGIN` opens the existing journey flow.
3. Audio unlock/start is called exactly once.
4. `TAP TO BEGIN` makes **zero** fullscreen requests.
5. Home and global Mini Player appear.
6. Thai personalized greeting renders `ยินดีต้อนรับ Benz` without the English comma.
7. Settings opens and Home hides correctly.
8. Language changes to English and renders `Welcome, Benz` correctly.
9. Mini Player options expand.
10. Next-track control remains wired and track title updates.
11. Thai language can be restored.
12. Return-to-Title confirmation works.
13. Returning to Title stops audio and hides the app session.
14. No unexpected page runtime errors occurred in the simulation.

## 6. Responsive layout simulation — 150 / 150 PASS

Headless Chromium rendered TH and EN Home compositions at:

- 320 × 700
- 360 × 800
- 390 × 844
- 430 × 900

Validated programmatically:

- no horizontal overflow
- fixed Home header remains anchored after scroll
- header height remains within the intended compact range
- Mini Player does not overlap Bottom Navigation
- 360 px common Android width retains the premium two-column paired-card layout
- 320 px narrow width falls back to stacked Primary/Signature cards
- paired title blocks stay optically balanced
- text copy remains contained inside all tested cards
- motif opacity remains within the restrained premium range
- user can scroll to the true bottom
- at maximum scroll, Explore content clears the fixed Mini Player by at least ~48 CSS px at 320/360/390 test widths

## 7. Visual design decision

V0.3.6 deliberately keeps the successful V0.3.5 architecture and performs refinement instead of another redesign:

- slightly shorter/calmer header
- soft fade edge below the fixed header
- tighter intro rhythm
- refined TH/EN card typography
- simplified Ask Ganesha / Daily / Three-Card / Golden Path / Obstacle motifs
- slightly stronger Explore icon presence
- more compact Mini Player while preserving all controls

## 8. Pre-deployment validation honesty / remaining gate at package creation

This build has passed the strongest pre-deployment validation available in the current environment. It has **not** been declared pixel-perfect on every real device before deployment.

One post-deployment real-device visual gate is still required because these cannot be perfectly reproduced by the simulation:

- Android/iOS font rasterization
- real device DPR
- browser chrome dimensions
- installed-PWA safe-area behavior
- platform-specific text shaping
- GPU/backdrop-filter differences

If the deployed screenshots remain visually stable, V0.3.6 can be promoted to the canonical Home baseline and Reading Engine work may begin.

## 9. Final package integrity

After the test suites above were re-run successfully, the upload archive itself was validated:

- static / structural suite re-run: **198 / 198 PASS**
- application-flow suite re-run: **22 / 22 PASS**
- responsive-layout suite re-run: **150 / 150 PASS**
- ZIP CRC/integrity (`testzip`): **PASS**
- archive root structure: **PASS** — no wrapper directory
- extracted patch byte-for-byte comparison with release source: **PASS**
- overlay simulation over verified V0.3.5 baseline: **PASS**
- protected-file byte equality after overlay: **PASS**
- prohibited protected assets absent from patch archive: **PASS**
- extracted `js/app.js` syntax: **PASS**
- extracted `sw.js` syntax: **PASS**

`PATCH_MANIFEST_V0_3_6.json` records the verified baseline commit and per-file SHA-256 hashes. `SHA256SUMS_V0_3_6.txt` provides a human-readable checksum list for the release files.


## 10. Post-deployment Android real-device gate — PASS

After deployment of commit `d7c6fb7657fa2cb88d7ed0a6194d7439c959f4bc` (`Polish Home UI to V0.3.6`), P’Benz supplied real-device Android evidence for the deployed build.

Verified from supplied evidence and product review:

- Thai Home composition renders with the intended hierarchy,
- English Home composition renders with the intended hierarchy,
- sacred motifs are visible but subordinate to copy,
- Mini Player and Bottom Navigation are visually coherent,
- fixed Home header no longer exhibits the rejected V0.3.3/V0.3.4 typography/symbol regressions,
- personalized Thai greeting renders without the English comma,
- launcher/Home Screen displays the canonical Little Ganesha App Icon instead of the old generic gray `G`,
- installed-app presentation appears app-like in the supplied Android evidence.

**Decision:** Home Visual System V0.3.6 = APPROVED / CANONICAL.  
**Decision:** Final App Icon = APPROVED / CANONICAL.

### Remaining cross-platform boundary

iPhone/iPad Add-to-Home-Screen and standalone real-device behavior have not yet been supplied as real-device evidence. Therefore:

- Android real-device product gate: PASS
- full iOS/Android cross-platform QA-X: PENDING iOS/iPadOS validation

This limitation does not block Reading Engine development because V0.3.6 runtime/PWA source is now a protected stable baseline.
