# QA — V0.5.2 Reading Discipline + Smart Reset

**Baseline:** V0.5.1 / GitHub HEAD `ed38c109ef134c553d247a2e574be8b43f7f969f`

## Automated checks

1. JavaScript syntax passes for all delivered runtime and test files.
2. Reading Engine content/state/persistence regression passes; engine stays 1.1.0.
3. Compact Deck Ritual regression passes: 78 pre-shuffled positions, six rows, no horizontal-scrolling regression.
4. Reading Day local-date and next-local-midnight calculations pass.
5. Quiet Countdown formatting passes for hours/minutes, minutes-only, and final-minute seconds in native Thai and English labels.
6. Countdown scheduling suspends while hidden and recalculates on visibility/pageshow; an idle module establishes a fresh date baseline to avoid a false rollover after midnight.
7. Ask Question Guard and Question Analyzer regressions pass (344+ analyzer cases).
8. Ask Context bilingual matrix passes (1,404 readings).
9. Semantic Ask no-drift bilingual matrix passes (1,404 readings).
10. Ask semantic duplicate lock passes 30 curated TH/EN/cross-language positive and negative cases, including wealth, income, promotion, financial stability, self-image, social perception, choice, burnout, healing, spiritual path/protection, project success, relationship future, reconciliation, and named-person separation.
11. Semantic duplicate restoration keeps the same card and identical user-facing answer fields in both Thai and English.
12. Materially different timeframe, person, or topic does not match in the curated negative cases.
13. Ask storage still contains no raw question text and remains compatible with prior same-day records.
14. Three-Card persistence refuses to lock an incomplete spread, saves only an interpreted spread, restores three unique cards, reproduces the same deterministic narrative, and expires on a new local day.
15. Three-Card narrative regression passes 1,456 bilingual samples.
16. Save/Share standard regression passes.
17. Global TH/EN copy regression passes.
18. Home profile / exact age / zodiac regression passes.
19. Three-Card package wiring and current package wiring pass.
20. Repository structure policy passes.
21. V0.5.2 application-version, app build, service-worker build, script query strings, and new runtime cache entries are coherent.
22. Existing V0.5.1 audio visibility behavior was verified directly in the baseline source and intentionally left unchanged.

## Real-device acceptance after deployment

- Ask the same question verbatim: same card and same reading.
- Rephrase the same question naturally in Thai or English: same card and same reading.
- Change the actual topic, person, or meaningful timeframe: a new reading is allowed.
- Complete one Three-Card spread, leave, return: the same spread and reading return without a new draw.
- Leave Three-Card before completing interpretation: returning must not be daily-locked.
- Daily and Three-Card show a restrained countdown; a restored duplicate Ask question shows its own countdown.
- Cross local midnight with the app foregrounded or backgrounded: availability refreshes from real local time without requiring a reload.
- Music playing → background → pause → foreground → resume from the same position.
- Music manually paused → background → foreground → remains paused.
- Compact 78-card V0.5.1 layout remains visually unchanged and comfortable to tap.

**Status before deployment:** automated QA PASS · checksum PASS · archive re-extraction PASS · real-device acceptance pending.
