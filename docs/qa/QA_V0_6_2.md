# QA — V0.6.2 Golden Path Editorial Layout

## Scope

Presentation-only refinement of Golden Path revealed-card blocks plus required coherent runtime/cache identity update.

## Static gates

- `css/golden-path.css` retains balanced braces and remains inside the existing `reading` cascade layer;
- Golden Path overrides are scoped under `.golden-shell` and do not alter Three-Card result-card presentation;
- Golden Path revealed-card grid is forced to one column at all viewport widths;
- revealed card artwork is centered and constrained to a mobile-appropriate width;
- interpretation text occupies full available block width;
- Thai typography receives dedicated line-height tuning;
- magnifier cue remains present and interactive behavior is not changed;
- `index.html`, `js/app.js`, and `sw.js` use runtime 0.6.2 coherently;
- no live `?v=0.6.1` query remains in `index.html` or `sw.js`;
- Reading Engine, Deck Ritual, Golden narrative/storage/export/UI/viewer JavaScript are untouched;
- current root Patch Manifest policy is V0.6.2 only after superseded manifests are deleted;
- current-release checksums are regenerated after final file generation.

## Real-device acceptance checklist

1. Reopen today's completed Golden Path reading; the same Focus, same three cards and same conclusion must restore.
2. Confirm each revealed card is displayed as a calm full-width block rather than a narrow side-by-side split.
3. Confirm artwork is centered, prominent but not oversized, and the card title hierarchy is clear.
4. Confirm Thai interpretation copy uses the full block width and feels materially easier to read with natural line wrapping.
5. Switch to English and confirm equivalent balance and comfortable line length.
6. Tap the magnifier on all three cards and verify the correct artwork opens enlarged.
7. Close the viewer with × and backdrop tap and confirm reading scroll position remains stable.
8. Verify Save Image and Share still operate.
9. Confirm Quiet Countdown remains visible after completion.
10. Leave and reopen Golden Path again on the same local day; the same result must remain locked.
11. Smoke-test a previously completed Three-Card Reading and confirm its existing result-card layout is unchanged.
12. Smoke-test Daily Guidance and Ask Ganesha for normal entry/operation.
13. Confirm background/minimize/screen-lock audio behavior remains unchanged.
14. Confirm visible build/runtime is V0.6.2 after the PWA refreshes.

Do not label V0.6.2 real-device PASS until these checks are actually observed on a device.
