# QA — V0.15.0

**Release:** Help & Feedback + Tarot Library Search Fix  
**Baseline:** V0.14.0  
**Rollback:** `3effae89a2f3fddfed0914b38250629aeefbffd7`

## Static/package QA — PASS

- `node --check js/app.js`
- `node --check js/tarot-library-ui.js`
- `node --check js/help-feedback.js`
- `node --check sw.js`
- `manifest.webmanifest` parses as JSON
- `index.html` runtime metadata/body/query strings/build label are V0.15.0
- `window.LGT_BUILD` is V0.15.0
- Service Worker BUILD/cache identity is V0.15.0
- Service Worker shell includes `css/help-feedback.css` and `js/help-feedback.js`
- all runtime CSS/JS query strings in `index.html` are V0.15.0
- no `renderDeck(true)` search-input recreation path remains
- no cursor-restoration `.setSelectionRange(...)` workaround remains
- input events update deck results through `refreshDeckResults()`
- support email is exactly `benedict.support@gmail.com`
- report email is user initiated through `mailto:`
- diagnostic generator does not access Ask/Journal/profile/payment data
- EN/TH/HI Help & Feedback copy objects are present
- Help & Feedback module initializes under a minimal DOM/Android-PWA VM stub and emits the expected diagnostic fields
- package SHA-256 verification passes after ZIP extraction

## Protected regression contract

No source file for Reading Engine, Deck Ritual, reading storage/selection, Lucky Numbers, Journal, Reading Hub, PromptPay, audio lifecycle, legal acknowledgement, Save/Share or card viewers is functionally edited by this patch.

`js/app.js` changes only the runtime build marker.

## Real-device acceptance — REQUIRED / NOT YET CLAIMED

### Tarot Library search

1. Android installed PWA: tap Search once, type at least 10 English characters continuously; keyboard must remain open.
2. Thai: type and delete a multi-syllable search phrase continuously; no keyboard collapse or broken composition.
3. Hindi: type Devanagari text using an IME; composition must remain stable.
4. Use Backspace repeatedly.
5. Paste text into Search.
6. Use native clear/search control.
7. Change a suit/filter, return to Search and continue typing.
8. Open a matching card and navigate back; query/results must remain coherent.

### Help & Feedback

1. Settings shows Help & Feedback before Support the Project.
2. Report a Problem opens correctly in EN/TH/HI.
3. Report technical details default ON and preview matches the device/build.
4. Feedback technical details default OFF.
5. Send opens the device email workflow addressed to `benedict.support@gmail.com` with subject/body prefilled.
6. No email is sent until the user sends it in their email client.
7. Copy Diagnostic Info works.
8. Copy Report Details works.
9. Cancel/backdrop/Escape closes the dialog and returns focus safely.
10. Verify Settings scrolling and safe-area behavior on a small phone.

### PWA update

1. Existing installed V0.14.0 updates to V0.15.0 without mixed cached runtime assets.
2. Relaunch after update and verify visible BUILD 0.15.0.

**QA status:** STATIC/PACKAGE QA PASS — real-device/browser interaction acceptance pending.
