# QA — V0.11.1 Journal Numeral Polish

## Scope

Focused typography correction for Journal numeric counters after real-device review. No Journal storage schema, capture logic, reading source state, deletion isolation, tarot selection, Lucky Numbers selection, audio lifecycle or Tarot Library behavior is changed.

## Static checks

- `js/journal-ui.js` parses successfully.
- `js/app.js` and `sw.js` parse successfully.
- Runtime/build markers are coherently `0.11.1` in HTML metadata/body/build label, `window.LGT_BUILD`, manifest icon URLs and Service Worker build/cache/application-shell URLs.
- Journal count surfaces use `.journal-numeral`.
- `.journal-numeral` uses `Georgia, "Times New Roman", serif`, lining numerals and tabular numerals.
- Per-day saved-entry count wraps its digit independently from localized prose.
- Monthly stats, card counts, calendar day/count markers, pattern/monthly summaries and selection count use the same numeral treatment.
- Lucky Numbers source files are not modified by this patch.
- Protected Reading Engine / Deck Ritual / reading storage files are not included in the patch.

## Package checks

- Direct-upload ZIP contains only files that must be added/replaced.
- SHA-256 manifest verifies after a fresh unzip.

## Device acceptance

Pending. Confirm especially that a Journal day containing exactly one saved entry displays **1**, not a Roman-looking **I**, in EN / ไทย / हिन्दी.
