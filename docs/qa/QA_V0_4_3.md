# QA Report — V0.4.3 Save & Share Reading

## Scope verified

- version/build marker update to `0.4.3`
- Daily Guidance UI wiring for save/share controls
- TH/EN copy coverage for new save/share flow
- export image generation code presence and syntax
- repository-structure compliance for current delivery
- current-version release / QA / checksum docs placement

## Validation performed

### Automated/static
- `node --check js/reading-ui.js` → PASS
- `node docs/tests/test-package.js` → PASS
- `node docs/tests/test-global-copy.js` → PASS
- `node docs/tests/test-repo-structure.js` → PASS

### Manual code review
- Save button triggers export generation → PASS
- Share button uses Web Share API when file sharing is supported → PASS (code path verified)
- Share fallback saves image when direct sharing is unavailable → PASS (code path verified)
- Export content excludes media-player/navigation UI → PASS
- Export content includes card + meaning + reflection + six Daily Lenses → PASS

## Pending real-device QA

The following still need verification after upload on an actual deployed build:

- Android Chrome: save path behavior
- Android Chrome: native share sheet behavior (e.g. LINE)
- installed PWA mode: save/share behavior
- iOS Safari/PWA compatibility, if targeted

## Risk assessment

**Risk:** Medium

Reason:
- new client-side canvas export flow
- native share behavior depends on browser/device capability
- runtime build and service worker cache markers changed to `0.4.3`
