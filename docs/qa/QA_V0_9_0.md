# QA — V0.9.0 Lucky Numbers

## Automated/static release gates

- [x] Baseline V0.8.1 runtime files verified against current GitHub package checksums before modification.
- [x] All changed/new JavaScript passes `node --check`.
- [x] Lucky content contains complete EN / TH / HI copy and number profiles 1–9.
- [x] Every number profile contains keyword, Core, Supporting and Balancing guidance.
- [x] Random generator returns exactly 3 unique integers from 1–9.
- [x] Same-day `createToday()` restores rather than rerolls.
- [x] Completion persists and restores the exact stored numbers.
- [x] Machine state test confirms reveal order matches stored number order and emits one completion.
- [x] Machine emits one spin phase, one reveal phase and three ordered reveal callbacks.
- [x] Lucky scripts load after shared Reading Export and before no dependent consumer.
- [x] `index.html`, `js/app.js`, `manifest.webmanifest` and `sw.js` are coherent at V0.9.0.
- [x] All versioned local references in `index.html` use `?v=0.9.0`.
- [x] All versioned app-shell references in `sw.js` use `?v=0.9.0`.
- [x] Service Worker app shell includes Lucky CSS and all six Lucky JS modules.
- [x] Protected `js/reading-engine.js` and `js/deck-ritual.js` are excluded from the upload patch.
- [x] Rolling SHA-256 package checksum generated and reverified after ZIP extraction.

## Real-device acceptance — required after upload

### Core interaction

- [ ] Lucky Numbers home card opens the real feature and does not show the old Coming Soon toast.
- [ ] First visit shows an idle luxury oracle machine with nine numbered orbs.
- [ ] Start action visibly rotates/mixes the machine and releases exactly three balls.
- [ ] Revealed numbers match the result cards in the same order.
- [ ] Core / Supporting / Balancing roles are easy to understand without numerology knowledge.
- [ ] Animation feels responsive and premium rather than arcade-like.

### Persistence

- [ ] Leave before reveal finishes, reopen, and confirm the same already-selected set continues.
- [ ] Finish the reveal, leave, reopen, and confirm the exact three numbers restore.
- [ ] Replay reveal does not change any number.
- [ ] After local midnight, a new set becomes available.

### Language

- [ ] English copy is fully natural and no Thai/Hindi leaks appear.
- [ ] Thai copy is fully natural and no English/Hindi leaks appear outside the canonical brand lockup.
- [ ] Hindi copy is fully natural and no English/Thai leaks appear outside the canonical brand lockup.
- [ ] Devanagari and Thai line wrapping remain comfortable at 360–390 px widths.
- [ ] Brand masthead remains visually identical across EN / TH / HI.

### Motion / audio / lifecycle

- [ ] Reduced Motion produces the short sequential reveal without high-motion mixing.
- [ ] Lucky SFX is silent when app sound is disabled.
- [ ] Lucky SFX/haptics never interrupt or alter the saved music volume setting.
- [ ] Background/minimize/lock still follows the protected Audio V0.5.3 lifecycle.

### Save / Share

- [ ] Save generates a readable portrait image with exact stored numbers.
- [ ] Share opens native share when file sharing is supported; fallback saves correctly.
- [ ] EN export is native English.
- [ ] TH export is native Thai and renders Thai glyphs correctly.
- [ ] HI export is native Hindi and renders Devanagari glyphs correctly.
- [ ] Export includes the symbolic/non-gambling framing.

### PWA deployment

- [ ] Installed PWA activates V0.9.0 after close/reopen or normal refresh/update flow.
- [ ] No mixed V0.8.1/V0.9.0 cache state remains after activation.
- [ ] Existing five reading modes still open and complete normally.
