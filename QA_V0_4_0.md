# Little Ganesha Tarot — QA V0.4.0

**Target:** V0.4.0 Reading Engine Foundation / Daily Guidance vertical slice  
**Master Plan:** V3.7  
**Repository inspected:** `grolygori789-crypto/little-ganesha-tarot` / `main`  
**Repository HEAD before implementation:** `8ceb93630cb3daf79cb77fea85538f14a11e8db5` — `Update Master Plan and migration handoff V3.7`  
**Last canonical runtime implementation:** `d7c6fb7657fa2cb88d7ed0a6194d7439c959f4bc` — V0.3.6

## 1. Risk and restore governance

Final risk classification: **HIGH**.

Reason: although the new Reading subsystem is isolated, V0.4.0 changes `sw.js` build/cache/app-shell wiring. Master Plan V3.7 classifies Service Worker/cache strategy changes as HIGH risk.

The earlier internal MEDIUM assessment was corrected before release packaging. Restore Protocol was completed before this candidate was marked verified for upload.

Rollback target:

- canonical runtime commit: `d7c6fb7657fa2cb88d7ed0a6194d7439c959f4bc`
- runtime build: `0.3.6`

Restore archive verifies the exact canonical Git blob identities:

- `index.html` → `d16c8d0e1dbb6233cb40f6d3db2a66788b69f0bc`
- `js/app.js` → `7b23d3ff87876bae54675cca05c07ae149ad6f22`
- `sw.js` → `72bba80fafe0666fdfa206f1d142f2f955fb4f3f`

## 2. Changed-file scope

### Replaced runtime files

- `index.html`
- `js/app.js`
- `sw.js`

### Added runtime files

- `css/reading.css`
- `js/reading-content.js`
- `js/reading-engine.js`
- `js/reading-ui.js`

### Added/replaced release documentation

- `README.md`
- `RELEASE_NOTES_V0_4_0.md`
- `QA_V0_4_0.md`
- `PATCH_MANIFEST_V0_4_0.json`
- `PATCH_UPLOAD_NOTES.md`
- `CHECKSUMS_V0_4_0.sha256`
- `docs/test-reading-engine.js`
- `docs/test-package.js`

Protected runtime modules/assets are intentionally excluded from replacement.

## 3. QA-S — static / structural validation

**PASS**

Performed checks include:

- JavaScript syntax validation with Node `--check` for all candidate JS, Service Worker, and QA scripts,
- HTML parsing and duplicate static-ID inspection,
- CSS structural brace-balance validation,
- version/build marker coherence across HTML/app/Service Worker,
- cache-bust query coherence at V0.4.0,
- Reading Engine app-shell resource wiring,
- explicit check that the full 78-card PNG deck is **not** install-time pre-cached,
- Reduced Motion wiring inspection,
- artwork-failure fallback wiring inspection,
- lifecycle/focus/inert safety wiring inspection,
- legacy Home `featureSoon` handler suppression path inspected through capture-phase Daily handler,
- protected-file reverse-diff identity verification.

### Protected replacement identity result

Reversing only the intentional V0.4.0 additions/version markers reproduces canonical V0.3.6 Git blobs exactly:

- `index.html`: PASS
- `js/app.js`: PASS
- `sw.js`: PASS

This provides strong evidence that V0.3.6 protected markup/app logic/cache behavior was not silently rewritten beyond the isolated V0.4.0 integration points.

## 4. Reading content / tarot integrity validation

**PASS**

`docs/test-reading-engine.js` validates:

- exactly 78 cards,
- stable sequential IDs `00–77`,
- unique IDs and image paths,
- exact canonical card sequence,
- Strength = index/number VIII,
- Justice = index/number XI,
- Wands → Cups → Swords → Pentacles order,
- Ace → Two ... Ten → Page → Knight → Queen → King order,
- exact canonical filename mapping for all 78 card fronts,
- canonical master card-back path,
- Major/Minor metadata consistency,
- EN/TH title, keyword, upright meaning, reflection presence,
- reversal-ready fields present for both languages,
- no duplicate upright meaning or reflection text within a language,
- basic deterministic/high-risk claim guard against guaranteed lottery/medical/death framing.

## 5. Reading Engine runtime/unit execution

**PASS in Node execution; this is not QA-R browser validation.**

Validated:

- spread registry contains Daily / Ask / Three / Golden / Obstacle,
- expected position IDs and card counts,
- Ask Ganesha preserves question-input architecture,
- V0.4.0 stays upright-first,
- secure random integer remains in bounds over repeated execution,
- 78-card unique draw returns all 78 without duplicates,
- exclusions are respected,
- Daily choice candidates are unique,
- state machine follows valid transitions,
- invalid state transition is rejected,
- selected card matches chosen candidate,
- explicit upright orientation is persisted,
- same local day restores the exact same card,
- new local day allows a new Daily reading,
- corrupted local-storage JSON fails safely and clears,
- unsupported schema version fails safely and clears.

## 6. Browser QA attempt

**NOT COMPLETED / NOT CLAIMED AS PASS.**

The environment contains Chromium and Playwright. Biu attempted an actual integration run for Title → onboarding → Home → Daily Guidance → choose → reveal → same-day reload → Thai reload → Escape close and mobile/desktop viewport checks.

Both localhost and file navigation were blocked by the environment/browser policy with:

`ERR_BLOCKED_BY_ADMINISTRATOR`

Because the page could not be navigated to under automation, no browser visual/interaction result is claimed. This limitation is recorded rather than substituting simulated success.

## 7. PWA/Service Worker QA status

Static Service Worker coherence: **PASS**.

Verified statically:

- `BUILD = 0.4.0`,
- shell/runtime cache names derive from V0.4.0,
- HTML/CSS/JS query versions align,
- new Reading resources and card back are in shell,
- high-resolution card fronts remain runtime-cached only,
- pre-existing network-first code/script/style policy remains byte-identical after reversing isolated V0.4.0 additions,
- pre-existing stale-cache cleanup logic remains protected.

Still required after deployment:

- Service Worker install/activate on deployed origin,
- old V0.3.6 cache cleanup,
- online update behavior,
- offline shell reopen,
- offline Daily behavior for a previously cached selected card,
- behavior when an uncached card front is unavailable (premium fallback should appear),
- installed-PWA reopen/update behavior.

## 8. Required post-upload real-device gate

### Android

- Title → Home baseline regression,
- TH/EN switching,
- Settings/Profile/Motion,
- music Mini Player controls,
- Return to Title audio lifecycle,
- Daily Guidance first-use flow,
- choose one of three cards,
- reveal/flip smoothness,
- interpretation readability,
- same-day reopen returns same card,
- reload/close-reopen behavior,
- new-day boundary when practical to simulate safely,
- Reduced Motion,
- no hidden overlay/tap interception,
- PWA update/cache behavior.

### iPhone/iPad

Still pending from earlier builds and required for cross-platform completion:

- Add to Home Screen / standalone launch,
- safe areas,
- Daily reading flow,
- card flip/scroll/focus,
- audio continuity,
- Service Worker update behavior.

## 9. Current QA classification

- **QA-S:** PASS
- **Node unit/runtime logic:** PASS
- **QA-R:** NOT COMPLETED
- **QA-X:** PENDING
- **QA-P:** PENDING
- **Archive integrity:** PASS
- **Restore Protocol:** PASS

## 10. Release decision at this stage

Final archive/checksum validation passed. V0.4.0 is **verified for upload** at the documented QA-S/package-integrity level.

It must **not** yet be described as canonical, fully tested, cross-platform validated, or production-complete.

Master Plan V3.7 intentionally remains unchanged in this pre-deploy package because V0.3.6 is still the last canonical runtime baseline. After V0.4.0 is pushed, re-read from GitHub, and passes the appropriate deployed/real-device gate, canonical-status documentation can be promoted in a controlled follow-up documentation update.
